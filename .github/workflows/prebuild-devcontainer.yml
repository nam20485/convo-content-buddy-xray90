name: Pre-build dev container image

on:
  workflow_run:
    workflows: [Publish Docker]
    types:
      - completed
  schedule:
    - cron: "0 0 * * 1" # Weekly on Monday at midnight UTC
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/devcontainer
  VERSION_IMAGE_TAG: "${{ vars.VERSION_PREFIX }}.${{ github.run_number }}"

jobs:
  prebuild:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Checkout repository
        # trunk-ignore(yamllint/line-length)
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pre-build dev container image
        uses: devcontainers/ci@8bf61b26e9c3a98f69cb6ce2f88d24ff59b785c6
        id: prebuild
        with:
          subFolder: .github
          imageName: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          cacheFrom: ${{ env.REGISTRY }}/${{ github.repository }}
          imageTag: ${{ github.ref_name }}-latest,${{ github.ref_name }}-${{ env.VERSION_IMAGE_TAG }}
          push: never

      - name: Strip stale devcontainer metadata before push
        env:
          VERSIONED_IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-${{ env.VERSION_IMAGE_TAG }}
          LATEST_IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-latest
        run: |
          # Remove accumulated devcontainer.metadata labels so downstream
          # consumers don't replay stale onCreateCommand entries
          for IMG in "$VERSIONED_IMAGE_FULL" "$LATEST_IMAGE_FULL"; do
            echo "FROM $IMG" | docker build --label devcontainer.metadata='[]' -t "$IMG" -
          done

      - name: Push images to registry
        env:
          VERSIONED_IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-${{ env.VERSION_IMAGE_TAG }}
          LATEST_IMAGE_FULL: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-latest
        run: |
          set -euo pipefail
          docker push "$VERSIONED_IMAGE_FULL"
          docker push "$LATEST_IMAGE_FULL"
